@model SportEditVm
@{
	ViewData["Title"] = "New or Edit sport";
}
<form method="post" asp-controller="Sports" asp-action="UpSert">
	<input type="hidden" asp-for="SportId"/>
	<div class="border p-3 mt-4">
		<div class="row pb-2">
			<h2 class="text-primary">@(Model.SportId==0?"Adding new sport":"Edit sport")</h2>
		</div>
		<div asp-validation-summary="ModelOnly" class="text-danger"></div>
		<div class="mb-3">
			<label asp-for="SportName">Sport name</label>
			<input asp-for="SportName" class="form-control" />
			<span asp-validation-for="SportName" class="text-danger"></span>

		</div>
		
			@if (Model.SportId==0)
			{
				<button type="-submit" class="btn btn-success">
				<i class="bi bi-floppy"></i>
					Save
				</button>
			}
			else
			{
				<button type="-submit" class="btn btn-success">
				<i class="bi bi-floppy"></i>
					Edit
				</button>
			}
			
		<a asp-controller="Sports" asp-action="Index" class="btn btn-info">
			<i class="bi bi-x-square"></i>
			Cancel
		</a>
		
	</div>
</form>
@section Scripts{
	@{
		<partial name="_ValidationScriptsPartial"/>
	}
}